type Profile = {
    id: string
    email: string
    name: string
    subscription_tier: string
    org_id: string
    uploads_used: number
    uploads_limit: number
    extractions_used: number
    extractions_limit: number
    is_admin: boolean
    created_at: string
  }